name: acadia-job

services:
  app:
    build:
      context: .
    image: acadia-job-image
    container_name: acadia-job-container
    ports:
      - "8000:8000"  # Host:Container
    volumes:
      - .:/app

  #   depends_on:
  #     - nginx

  # nginx:
  #   image: nginx:latest
  #   container_name: nginx_proxy
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
  #     - ./staticfiles:/app/staticfiles
  #     - ./media:/app/media
  #   depends_on:
  #     - app
